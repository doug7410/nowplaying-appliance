# Secrets — the shared appliance token lives here, never commit it
src/secrets.h

# Sim build artifacts + downloaded single-header deps
sim/build/
sim/third_party/

# Pi build artifacts + the runtime config (holds the shared appliance token)
pi/build/
pi/np.env
